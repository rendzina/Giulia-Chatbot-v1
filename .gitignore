# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
notes/

# Environment and secrets
.env
.ENV
*.local

# Local data (indices, manifests, persisted state) — keep empty folder in repo
data/*
!data/.gitkeep

# IDEs
.idea/
.vscode/
*.swp
.DS_Store

# Chainlit: ignore generated files but keep a versioned UI config
.chainlit/*
!.chainlit/config.toml

# Input files (PDF/Docx/txt) in corpus (user content — optional: remove if you want to track in git)
SourceDocuments/*
PDFPreProcessing/*