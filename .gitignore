# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Environment and secrets
.env
.ENV
*.local

# Local data (indices, manifests, persisted state) — keep empty folder in repo
data/*
!data/.gitkeep

# IDEs
.idea/
.vscode/
*.swp
.DS_Store

# Chainlit (local dev generates translations and local config; remove if you want to version config)
.chainlit/

# PDFs in corpus (user content — optional: remove if you want to track in git)
SourceDocuments/*.pdf
